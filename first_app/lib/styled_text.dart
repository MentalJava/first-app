import 'package:flutter/material.dart';

class StyldeText extends StatelessWidget {
  const StyldeText(this.text, {super.key});

  final String text;

  @override
  Widget build(context) {
    return Text(
      text,
      style: const TextStyle(
        color: Colors.white,
        fontSize: 28,
      ),
    );
  }
}
